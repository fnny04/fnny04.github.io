import { CreateCaseStudyModule } from "@/modules";
import { NextPage } from "next";
import { ReactElement } from "react";

const CreateCaseStudyPage: NextPage = (): ReactElement => {
  return <CreateCaseStudyModule />;
};
export default CreateCaseStudyPage;
