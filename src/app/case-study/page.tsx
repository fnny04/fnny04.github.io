import { AllCaseStudy } from "@/modules";
import { NextPage } from "next";
import { ReactElement, Suspense } from "react";

const CaseStudy: NextPage = (): ReactElement => {
  return <AllCaseStudy />;
};

export default CaseStudy;
